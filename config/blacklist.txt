# 地址黑名单关键词(自定义追加)
# 用法:
#   - 一行一个关键词,匹配方式为"包含即排除"(不区分大小写,全半角归一)
#   - 命中关键词的地址不会出现在任何生成结果中(楼名/街道/完整地址任一命中即排除)
#   - 以 # 开头的行是注释,空行忽略
#   - 修改本文件后约 10 秒内自动生效,无需重启服务
# 注意:代码内置的各国机构规则(政府/学校/医院/银行/消防等)仍然生效,本文件是额外追加。
